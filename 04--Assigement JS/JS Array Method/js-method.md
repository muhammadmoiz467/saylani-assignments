# JavaScript Array Methods

- **push()** → last me add karta hai  
- **pop()** → last se remove karta hai  
- **unshift()** → start me add karta hai  
- **shift()** → start se remove karta hai  
- **splice()** → kahin se bhi add/remove  
- **slice()** → copy banata hai  

